<div class="col-md-4 mb-4"> <!-- Cada categoria ocupa 4 colunas -->
                <div class="card">
                    <div class="card-body text-center">
                        <h5 class="card-title"><%= categoria.nome %></h5> <!-- Nome da categoria -->
                        <p class="card-text"><%= categoria.descricao %></p> <!-- Descrição da categoria -->
                        <a href="#" class="btn btn-primary">Ver Mais</a>
                    </div>
                </div>
            </div>